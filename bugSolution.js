function add(a, b) {  return a + b; } function subtract(a, b) {  return a - b;} function multiply(a, b) {  return a * b;} function divide(a, b) {  if (b === 0) {    return 0; //Handle division by zero  }  return a / b;} let result; try {  result = add(5, 3);  result = multiply(result, subtract(10, 2));  result = divide(result, add(2, 2));} catch (error) {  console.error('An error occurred:', error.message);  result = 0; // Set a default value in case of an error} console.log(result); //The application continues to run even if an error occurs.